{
  "name": "the-advancement-tests",
  "version": "1.0.0",
  "description": "Integration tests for The Advancement app payment flows",
  "type": "module",
  "scripts": {
    "test": "mocha payment-flows.test.js --timeout 30000",
    "test:watch": "mocha payment-flows.test.js --timeout 30000 --watch",
    "test:verbose": "mocha payment-flows.test.js --timeout 30000 --reporter spec"
  },
  "dependencies": {
    "sessionless-node": "file:../../../sessionless/src/javascript/node",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  }
}
